<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.tester">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".activities.ToastyActivity">
        </activity>
        <activity android:name=".activities.ResizeLayoutActivity"/>
        <activity android:name=".activities.RecyclerViewActivity"/>
        <activity android:name=".activities.ImageRoundedActivity"/>
        <activity android:name=".activities.GraphActivity"/>
        <activity android:name=".BarChartActivity"/>
        <activity android:name=".BarChartActivityMultiDataset"/>
        <activity android:name=".activities.CropImageActivity"/>
        <activity android:name=".activities.QRCodeActivity"/>
        <activity android:name=".activities.DialogTestActivity"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
                android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>